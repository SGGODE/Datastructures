// int cnt=0;
        // int sum=0;
        // unordered_map<int,int>mp;
        // for(int i=0;i<n;i++){
        //   sum=sum^arr[i];
        //   if(sum==k)++cnt;
        //   if(mp.find(sum^k)!=mp.end())
        //       cnt+=mp[sum^k];
        //   mp[sum]+=1;
        // }
        // return cnt;
